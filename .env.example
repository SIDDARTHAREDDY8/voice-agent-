# Copy this file to `.env` and paste your key. `.env` is gitignored — never commit it.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional model overrides:
# MODEL_AGENT=claude-sonnet-5
# MODEL_SIM=claude-haiku-4-5-20251001
