anthropic>=0.40
pydantic>=2.0
# Self-hosted neural voice for the live button — free, no key, no quota.
piper-tts
# ZeroGPU startup check needs a @spaces.GPU function to exist (see app.py).
spaces
# Pin below the huggingface_hub 1.0 line — matches the version verified with the
# Space's Gradio (see sdk_version in README.md).
huggingface_hub<1.0
# gradio is provided by the Hugging Face Space (see sdk_version in README.md).
# For local dev: pip install "gradio>=5.50"
